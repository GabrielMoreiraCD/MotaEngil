# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Delineador Técnico Autônomo v1.0
# ─────────────────────────────────────────────────────────────────────────────

# ── Orquestração ──────────────────────────────────────────────────────────────
langchain>=0.2.0
langgraph>=0.1.0
langchain-community>=0.2.0

# ── Vector Store ──────────────────────────────────────────────────────────────
qdrant-client>=1.9.0          # produção (Cloud ou local Docker)
# Para POC sem Qdrant: usar rag/vector_store.py InMemoryVectorStore (incluso)

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0  # BAAI/bge-m3
langchain-huggingface>=0.0.3

# ── Extração de PDF ───────────────────────────────────────────────────────────
pymupdf>=1.24.0               # fitz — extração com filtro espacial Y
pytesseract>=0.3.10           # OCR para PDFs escaneados
Pillow>=10.0.0

# ── Parsing de planilhas ──────────────────────────────────────────────────────
openpyxl>=3.1.0               # XLSM/XLSX (data_only=True para VBA)
pandas>=2.0.0
xlrd>=2.0.1                   # XLS legados

# ── Modelos locais ────────────────────────────────────────────────────────────
# Ollama roda como servidor separado (ollama serve)
# HuggingFace para Qwen2.5-VL-72B (Inference API)
huggingface_hub>=0.23.0

# ── Validação ─────────────────────────────────────────────────────────────────
pydantic>=2.0.0

# ── Geração de output ─────────────────────────────────────────────────────────
reportlab>=4.0.0              # PDF Minuta de Materiais

# ── Utilitários ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0

# ── Pipeline BOM (Etapas 3-5) ─────────────────────────────────────────────────
anthropic>=0.28.0             # Claude API para extração de specs de normas (Etapa 3)